apply plugin: 'com.android.application'

android {
    compileSdkVersion = 25
    defaultConfig {
        applicationId "cn.asiontang.imageviewscaletypesample"
        minSdkVersion 1
        versionCode 1
        versionName "1.1"
    }
    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
        }
    }
}

dependencies {
}
